# ROUGE evaluation for fine-tuned Phi-3 Mini.
#
# Requirements:
#   - pip install rouge-score vllm
#   - Merge adapter first: python scripts/merge_adapter.py
#
# Usage:
#   bash scripts/run_eval.sh phi

model:
  model_name: "/workspace/summarization/outputs/model-phi-mini-merged"
  model_max_length: 4096
  torch_dtype_str: "bfloat16"
  trust_remote_code: true

generation:
  max_new_tokens: 256
  batch_size: 32

tasks:
  - evaluation_backend: custom
    task_name: rouge_summarization
    num_samples: 100
    eval_kwargs:
      dataset_path: "/workspace/summarization/MIMIC-IV-BHC/oncology-splits/test.jsonl"

inference_engine: VLLM

output_dir: "/workspace/summarization/outputs/eval-phi-mini"
